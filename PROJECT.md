# TgMon — карта проекта

Состояние исходников сверено 23 сентября 2026 года. Основной checkout: `C:\TgMon` / `/mnt/c/TgMon`. Репозиторий: `liquidsyntes/tg_mons`. Локальный web: [localhost:4000](http://localhost:4000).

## Назначение

Мониторинг и сравнение Telegram-каналов и групп: история аудитории, метрики публикаций, рекламная нагрузка, цитирование, эвристические сигналы риска и AI-анализ собранного контента.

## Устройство

- Next.js App Router, React и TypeScript: страницы/API в `src/app/`, UI в `src/components/`.
- Расчёты и запросы: `src/lib/`; Prisma-схема и миграции: `prisma/`.
- PostgreSQL хранит каналы, посты, снимки, дневные агрегаты, упоминания, сигналы, демографию и отчёты.
- Отдельный worker в `src/worker/` использует teleproto через npm alias `telegram`; расписание, сбор и сохранение отделены от UI. Ручной сбор и добавление канала выполняют общий сборщик также в web-процессе.
- Полный Compose содержит `postgres`, `web`, `worker`. Web и worker используют собранные образы; изменения исходников требуют пересборки соответствующих сервисов.

## Текущие возможности и ограничения

- ER/ERR/VR, CR групп, EP, Content Score, Ad Load и Citation Index; формулы и окна различаются по источникам.
- Антифрод: четыре эвристики, сохранение FraudSignal и аудит при чтении; балл риска не является вероятностью накрутки.
- Статьи Telegram преобразуются в обычный текст для просмотра, поиска и анализа. Частичные статьи догружаются, старые пустые записи восстанавливаются отдельной утилитой.
- Демография ограничена языковой статистикой при наличии прав; география не выводится из языка.
- AI использует OpenRouter и сохранённые тексты. Настройки провайдера/модели в UI пока не управляют фактическим клиентом; старые отчёты не обновляются после восстановления контента.
- Пользовательского входа в приложении нет; сетевой доступ требует отдельного ограничения.

## Где читать подробнее

| Вопрос | Документ |
| --- | --- |
| Установка и старт | [README](README.md) |
| Пользовательские сценарии | [Обзор](docs/overview.md) |
| Модули и файлы | [Карта кодовой базы](docs/codebase.md) |
| Потоки и данные | [Архитектура](docs/architecture.md) |
| Контракты HTTP | [API](docs/api-reference.md) |
| Формулы и неполные данные | [Аналитика](docs/analytics-formulas.md) |
| Docker, порт 4000 и health | [Эксплуатация](docs/deployment.md) |
| Восстановление статей и другие утилиты | [Scripts](scripts/README.md) |
| Решения | [Антифрод](docs/adr/0001-anti-fraud-detection-architecture.md), [Telegram-статьи](docs/adr/0002-telegram-rich-messages.md) |

Правила работы задают [AGENTS.md](AGENTS.md) и [GEMINI.md](GEMINI.md). Материалы прежних задач в `.agents/` — история работы, а не описание текущего продукта.
