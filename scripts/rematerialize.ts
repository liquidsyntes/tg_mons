import { prisma } from '../src/lib/prisma'; import { materializeDailyMetrics } from '../src/lib/materialize'; async function main() { const channels = await prisma.channel.findMany({ select: { id: true } }); for (const c of channels) { console.log('Materializing', c.id); await materializeDailyMetrics(c.id, 30); } console.log('Done'); } main().catch(console.error).finally(() => prisma.$disconnect());
