---
name: tg-monitor
description: Памятка по текущей реализации TgMon и навигации по документации проекта.
---

# Памятка по TgMon

Сверено 23 сентября 2026 года. Это краткий указатель на текущую реализацию, а не отдельное ТЗ. Правила работы: [AGENTS.md](AGENTS.md), [GEMINI.md](GEMINI.md), [рабочий процесс](docs/codex-workflow.md).

## Основные ориентиры

- Основной checkout — `C:\TgMon` / `/mnt/c/TgMon`; работать в нём с учётом незакоммиченных изменений.
- Web — Next.js App Router, React, TypeScript и Tailwind; компоненты собственные, зависимости shadcn/ui нет. Радиусы карточек и графиков — 3 px; аватары и индикаторы могут быть круглыми.
- БД — PostgreSQL с Prisma. Текущие модели и ограничения определяются [схемой](prisma/schema.prisma), а не первоначальным набором Channel/Snapshot/Post.
- Сборщик — teleproto (`telegram` в импортах, npm alias в package.json), MTProto-сессия в `TG_SESSION`. Bot API используется только для опциональных уведомлений.
- Docker-сервисы — `postgres`, `web`, `worker`; web публикует порт 4000. Полный Compose не подхватывает правки исходников без пересборки.

## Поведение, которое важно сохранять

1. Первый сбор: до 1000 сообщений за последние 30 дней; обычный цикл: до 200 последних сообщений без `min_id`, с обновлением счётчиков существующих записей.
2. Статьи: `richMessage` → догрузка при `part=true` → обычный текст в `Post.text`. Ошибка догрузки не затирает полный текст и не останавливает сбор остальных сообщений.
3. Retry — пауза перед вызовом и повтор при FLOOD_WAIT с указанным Telegram временем и jitter; распределённого лимитера и экспоненциального backoff в текущем коде нет.
4. Ошибки каналов изолируются; дополнительные Telegram-статистики не должны ломать основной сбор. Web также выполняет сбор при добавлении канала и ручном запросе.
5. Дневные агрегаты хранятся в БД. Их календарные UTC-окна отличаются от скользящих окон сырых постов; формулы нельзя выводить из подписей UI.
6. «Мой канал» назначается через UI/API, прежний флаг снимается транзакцией. Канал с этим флагом может отсутствовать; `MY_CHANNEL_USERNAME` не используется кодом.
7. Отключение сохраняет историю; постоянное удаление канала удаляет связанные данные по каскадам.
8. Middleware подставляет Bearer-токен, но не реализует пользовательскую аутентификацию. Не считать локальное приложение защищённым для публичного размещения.

## Навигация

- [Обзор продукта](docs/overview.md) и [карта модулей](docs/codebase.md).
- [Архитектура и потоки](docs/architecture.md), [формулы](docs/analytics-formulas.md), [HTTP API](docs/api-reference.md).
- [Запуск и эксплуатация](docs/deployment.md), [утилиты восстановления](scripts/README.md), [Git-процесс](docs/git-workflow.md).

Команды проверок берутся из package.json и CI; результаты следует получать текущим запуском. При изменении только документации достаточно проверки ссылок, кодировки, утверждений по исходникам и diff. Коммиты, push, слияния и обновление Docker выполняются по отдельному поручению пользователя.
