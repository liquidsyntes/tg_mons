import { prisma } from './src/lib/prisma'; prisma.aiReport.findFirst({ where: { type: 'super_report' }, orderBy: { id: 'desc' } }).then(report => { try { JSON.parse(report.content); console.log('JSON IS VALID'); } catch(e) { console.error('JSON IS INVALID:', e.message); console.log('--- CONTENT START ---'); console.log(report.content); console.log('--- CONTENT END ---'); } }).finally(() => prisma.$disconnect());
